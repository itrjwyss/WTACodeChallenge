<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aknowledgejourney.wtacodecallenge">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WTACodeChallenge"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WTACodeCallenge"
        tools:ignore="AllowBackup">
        <activity
            android:name=".presentation.character.detail.CharacterDetailActivity"
            android:theme="@style/Theme.WTACodeCallenge.NoActionBar"/>
        <activity
            android:name=".presentation.character.list.CharacterListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>